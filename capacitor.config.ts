import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.andrewprince.carculator',
  appName: 'CARculator',
  webDir: 'dist-web'
};

export default config;
